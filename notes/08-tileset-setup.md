# TileSet Setup

`resources/tilesets/room_tileset.tres` is a single shared `TileSet` used by
`TestRoom.tscn`'s `Ground` node (a `TileMapLayer`). This documents how it's
built so new rooms/tiles follow the same pattern.

## Current sources

- **Floor** — `assets/sprites/tilesets/grass.png` (a single 16x16 tile, atlas
  coord `(0,0)`). No collision.
- **Wall** — `assets/sprites/tilesets/walls/walls.png` (a 128x128 sheet, 16x16
  cells, 8x8 grid). We use one cell from the seamless solid-brick block at
  atlas coord `(1,5)`, with a full 16x16 collision polygon on physics layer 0
  (mapped to `project.godot`'s `2d_physics/layer_1` = `world`, matching the
  player's collision mask).

Only these two files are wired in. **Do not use any tileset/object file with
a "Premium Version!" watermark baked into it** (see the open license
question — `decor_16x16.png`, `floors/carpet.png`, `water1.png`, `plains.png`,
and `objects/objects.png` are confirmed watermarked as of this writing;
several other files in `assets/sprites/objects/` and `water*.png` haven't
been checked yet). Verify with the `Read` tool / open the file before
wiring anything new in.

## basicMap.tmj (not yet imported)

`assets/sprites/tilesets/basicMap.tmj` is a Tiled-authored 15x15 map (grass
base layer + a dirt-patch decoration layer) sitting alongside the other
assets, committed as source material only — nothing in the Godot project
references it yet. Its decoration layer depends on `plains.png`, which is
watermarked (see above), so it can't be imported as designed until that's
resolved. `TestRoom.tscn` currently uses the simpler generated room
described above instead.

## Why generated by script, not hand-authored

`TileSet`/`TileMapLayer` resources store tile data (`tile_map_data`) as a
packed binary array and per-tile collision polygons as nested sub-resource
properties — both are impractical and risky to hand-type correctly. This
tileset and the room layout in `TestRoom.tscn` were built by a one-off
GDScript run through `godot --headless --script`, using the `TileSet`,
`TileSetAtlasSource`, and `TileMapLayer` APIs (`add_source`,
`create_tile`, `set_collision_polygon_points`, `set_cell`, ...), then saved
with `ResourceSaver.save()` / `PackedScene.pack()`. The generator script
itself isn't committed (it was a scratch tool), but this pattern —
build tiles via the scripting API rather than hand-editing `.tres`/`.tscn`
— is the safe way to extend this further.

## Adding a new tile

1. Confirm the source image's license and check for watermarks first.
2. Add a new `TileSetAtlasSource` (or a new atlas coord on an existing one
   if it's from the same sheet) to the `TileSet` resource, via script or the
   Godot editor's TileSet panel (editing it by hand in the editor is fine
   once it's small enough to reason about visually — scripting is mainly
   valuable for bulk/initial setup).
3. If the tile should block movement, give it a collision polygon on
   physics layer 0 (the only physics layer currently defined on this
   `TileSet`), sized to match the visual bounds.
